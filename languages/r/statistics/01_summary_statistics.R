# Objetivo: calcular estatísticas descritivas básicas.
# Como executar: Rscript .\languages\r\statistics\01_summary_statistics.R
# Conceitos treinados: mean, median, sd, summary.
# Próximo exercício: plots/01_basic_plot.R

dados <- c(7.5, 8.0, 6.5, 9.0, 8.5, 7.0, 6.0, 10.0)

print(mean(dados))
print(median(dados))
print(sd(dados))
print(summary(dados))
